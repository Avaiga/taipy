class propagate extends shared {

    /**
     * Allows the value to be automatically propagated.<br/>The default value is defined at the application configuration level.<br/>If set to `True`, any change to the control's value is immediately reflected in the bound application variable.
     *
     * @type {bool}
     */
     propagate = "App config";

     /**
      * Indicates if this component is active.<br/>An inactive component provides no user interaction.
      *
      * @type {dynamic(bool)}
      */
     active = true;
}
